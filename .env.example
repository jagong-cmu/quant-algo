# Copy this file to .env and fill in your key (.env is gitignored).
#   cp .env.example .env
# Then edit .env and run:  python run.py
PENTPORT_API_KEY=pp_live_REPLACE_ME

# Optional: override the API base URL (defaults to https://pentport.com)
# PENTPORT_BASE_URL=https://pentport.com
